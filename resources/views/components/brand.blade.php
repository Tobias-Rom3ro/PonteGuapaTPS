<div class="flex items-center justify-center gap-3">
    <img src="{{ $logo }}" alt="{{ $name }}" class="h-10">
    <span class="text-xl font-bold dark:text-white text-primary-500 transition-colors duration-300">{{ $name }}</span>
</div>
